using System.IO;

namespace WodiLib.Test.IO
{
    internal static class IoTestDataConstants
    {
        /// <summary>テストディレクトリルート</summary>
        public static string TestWorkRootDir => $@"{Path.GetTempPath()}WodiLibTest";
    }
}
